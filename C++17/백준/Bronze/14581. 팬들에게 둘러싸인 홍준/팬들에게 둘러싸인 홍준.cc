#include <stdio.h>
#include <string.h>
int main(void) {
    char id[100];
    scanf("%s", id);
    printf(":fan::fan::fan:\n");
    printf(":fan::%s::fan:\n", id);
    printf(":fan::fan::fan:");
    
    return 0;
}
